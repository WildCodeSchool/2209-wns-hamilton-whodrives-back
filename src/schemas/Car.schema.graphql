type Car {
  id: ID!
  seat: Int!
  model: Model
  #option: Option
}

type Query {
  cars: [Car]
  car(id: ID!): Car
}

type Mutation {
  createCar(seat: Int!, modelId: Int, optionId: Int): Car
  updateCar(id: ID!, seat: Int, modelId: Int, optionId: Int): Car
  deleteCar(id: ID!): Car
}
