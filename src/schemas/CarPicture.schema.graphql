 scalar Upload
 type CarPicture {
    id: ID!
    path: String!
  }

  type Mutation {
    createCarPicture(carId: ID!, file: Upload!): CarPicture!
    deleteCarPicture(id: ID!): msg
  }
type msg{
  msg: String
}