type Roles {
  id: ID!
  name: String
}

type Query {
  Roles: [Roles]
  Role(id: ID!): Roles
}

type Mutation {
  createRole( name: String): Roles
  updateRole(id: ID!, name: String): Roles
  deleteRole(id: ID!): Roles
}
