type UserInfo {
  id: ID!
  city: String
  country: String
  age: Int
  address: String
  aboutId: ID
  profilPictureId: ID
}

type Query {
  getUserInfo(id: ID!): UserInfo
  getUserInfos: [UserInfo]
}

type Mutation {
  updateUserInfo(
    id: ID!
    city: String
    country: String
    age: Int
    address: String
  ): UserInfo
  createUserInfo(
    city: String
    country: String
    age: Int
    address: String
    profilPictureId: Int
  ): UserInfo
}
