type Receipts {
  id: ID!
  file_name: String
}

type Query {
  Receipts: [Receipts]
  Receipt(id: ID!): Receipts
}

type Mutation {
  createReceipts( file_name: String): Receipts
  updateReceipts(id: ID!,file_name: String): Receipts
  deleteReceipts(id: ID!): Receipts
}